we have a web verson but we do not sugest useing it on pngs that where made useing the python verson at all! [website](https://olibot1107-good-projects.github.io/imgfile/)
# how to use

First, install dependencies:
```bash
pip install -r requirements.txt
```

## GUI

To run the graphical application:
```bash
python app.py
```

## CLI

To run the command-line interface:
```bash
python cli.py
```
